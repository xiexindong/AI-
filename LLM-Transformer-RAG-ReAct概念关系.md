# LLM · Transformer · Prompt · RAG · ReAct 概念关系速查

> 五个最容易搞混的概念，一张图+一个类比全部说清。
> 一句话记忆：
> - **Transformer = 发动机**（核心技术/结构）
> - **LLM = 汽车**（产品）
> - **Prompt = 方向盘/指令**（怎么指挥大脑）
> - **RAG = 图书馆**（外挂知识）
> - **ReAct = 手脚 + 办事流程**（行动能力）
> - **AI Agent = 老司机**（组合起来的完整智能体）

---

## 一、五者是什么（一句话版）

| 概念 | 一句话 | 本质 |
|---|---|---|
| **Transformer** | 一种"基于自注意力"的神经网络结构，让每个词同时看到全句、互相吸收信息 | 模型结构（技术） |
| **LLM**（大语言模型） | 用海量文字训练出来的"能读能写"的大模型（GPT、DeepSeek、Llama...） | 产品/类别（按功能分） |
| **Prompt**（提示词） | 给 LLM 下达的"指令"，告诉它你是谁、要干什么、按什么格式回答 | 交互方式（指令层） |
| **RAG**（检索增强生成） | 回答前先去外部文档库查资料，再基于查到的内容回答 | 应用技术（外挂知识） |
| **ReAct**（推理+行动） | 让模型"边想边干"：思考→调工具→看结果→再思考，循环直到完成 | 应用范式（行动循环） |

---

## 二、整体关系图

```
┌─────────────────────────────────────┐
│          AI Agent（智能体）           │
│                                     │
│   ┌──────────┐   ┌──────────────┐   │
│   │  RAG     │   │  ReAct       │   │
│   │ 外挂知识  │   │ 思考+行动循环  │   │
│   └────▲─────┘   └──────▲───────┘   │
│        │                │           │
│   ┌────┴────────────────┴───────┐   │
│   │       Prompt（指令层）        │   │
│   │   ← 桥梁：一切外部信息都要    │   │
│   │     翻译成 Prompt 喂给大脑    │   │
│   └───────────▲─────────────────┘   │
│               │                     │
│   ┌───────────┴─────────────────┐   │
│   │       LLM（大脑/产品）        │   │
│   │    内部结构 = Transformer     │   │
│   └─────────────────────────────┘   │
└─────────────────────────────────────┘
```

```
技术栈视角（从底层到顶层）:

  Transformer（发动机：自注意力结构）
       │ 用来造
       ▼
  LLM（汽车：GPT / DeepSeek / 通义千问...）
       │ 通过 Prompt（方向盘/指令）指挥它
       ▼
  RAG（导航+资料库） + ReAct（驾驶操作）
       │ 组合成
       ▼
  AI Agent（老司机：能查、能想、能干活的完整智能体）
```

---

## 三、生活化类比速记

| 概念 | 类比 | 一句话场景 |
|---|---|---|
| Transformer | 发动机 | 所有车的动力核心，也能装轮船、发电机 |
| LLM | 汽车 | 装上发动机造出来的"能跑"的产品 |
| GPT / DeepSeek / Llama | 宝马 / 丰田 / 比亚迪 | 具体某个品牌型号 |
| ChatGPT | 整车 + 4S 店 | 产品形态 |
| Prompt | 方向盘 + 指令 | 告诉车去哪、怎么开、注意什么 |
| RAG | 图书馆 | 不知道就先去翻书 |
| ReAct | 手脚 + 办事流程 | 不会就动手干、打电话问 |
| AI Agent | 老司机 | 会查资料、会动手、会规划、能完成任务 |

---

## 四、Prompt 与四者的关系（核心逻辑）⭐⭐

### 4.1 一句话

> **Prompt（提示词）= 给 LLM 下达的"指令"。**
> LLM 是"吃指令"的模型：你喂给它什么，它就顺着干什么。
> 没有 Prompt，LLM 只是台"空引擎"——不知道要干嘛。

### 4.2 为什么它是"桥梁"？（最关键洞察）

**所有外部技术（RAG / ReAct / Function Calling）最终都要"翻译成 Prompt"才能让 LLM 执行。**

```
  RAG（检索结果：相关文档片段） ──┐
                                 ├──► 拼装成 Prompt ──► LLM（Transformer 大脑）──► 输出
  ReAct（格式约束：Thought/Action）─┘
```

| 技术 | 它怎么变成 Prompt |
|---|---|
| **RAG** | 把检索到的文档片段拼进 prompt：`请基于以下材料回答: [文档片段] 问题: ...` |
| **ReAct** | 用固定 prompt 模板约束输出格式：`你只能按 Thought / Action / Observation 格式输出` |
| **Function Calling** | 把工具清单（JSON Schema）写进 prompt：`你有这些工具可用: [{"name": "search", ...}]` |

**所以 Prompt 是把"外部能力"和"大脑"连接起来的那个唯一通道。**

### 4.3 Prompt 的组成（面试常考）

| 部分 | 作用 | 例子 |
|---|---|---|
| **System Prompt** | 设定角色 / 规则 / 边界 | "你是资深 MySQL DBA，回答要简洁，不确定就说不确定" |
| **User Prompt** | 给出具体任务 | "解释一下索引失效的 7 大场景" |
| **Context** | 喂给模型的背景资料 | RAG 检索出的文档、用户历史记录 |
| **Few-shot** | 给几个示例让模型模仿 | "例如：Q:... A:...（再来一个）" |

### 4.4 Prompt 的质量决定一切

同样的 LLM（同样的大脑），指令写得好坏，输出天差地别：

```
❌ 差: "帮我写个登录功能"
✅ 好: "用 React + TypeScript 写一个登录组件:
     输入框（用户名/密码）、校验规则（密码≥8位）、
     提交时调用 /api/login，错误提示显示在表单下方，
     代码要含注释，输出完整可用文件。"
```

### 4.5 Prompt 与 Agent 的关系

- Agent 的每次"思考→行动"循环，本质都是**不断构造新的 Prompt** 发给 LLM
- 系统提示词（System Prompt）就是 Agent 的"人设 + 规则"，决定 Agent 的行为边界
- 面试官问"怎么让 Agent 稳定输出 JSON"——答案就是写强约束的 Prompt（详见 [`大模型怎么稳定输出JSON`](./大模型怎么稳定输出JSON/)）

### 4.6 Prompt 速答

> Prompt 是用户与 LLM 交互的指令层，LLM 是吃指令的模型。RAG 的检索结果、ReAct 的格式约束、Function Calling 的工具描述，最终都要拼进 Prompt 才能驱动模型。所以 Prompt 是连接外部能力与大脑的桥梁，也是 Agent 开发中成本最低、见效最快的调优点。

---

## 五、Transformer 和 LLM 的关系（重点）

### 关系一句话

> **LLM 是"产品"，Transformer 是"制造它的核心技术"。**
> 绝大多数 LLM 都是用 Transformer 结构造的。

### 分类方式不同（面试必答）

- **LLM** 按"功能"分类：大（几十亿~上万亿参数）+ 语言（海量文本训练）+ 模型（能读写对话）
- **Transformer** 按"结构"分类：自注意力 + 层层堆叠
- 两者是"交叉"关系，不是"等于"关系

### 误区澄清

**误区 1："Transformer 就是 LLM"？**
不对。Transformer 是通用零件，还能用于：
| 领域 | 产物 |
|---|---|
| 文字 | LLM（GPT、BERT） |
| 图片 | ViT（视觉 Transformer） |
| 语音 | Whisper |
| 蛋白质 | AlphaFold |

**误区 2："LLM 必须是 Transformer"？**
也不绝对。新结构 Mamba、RWKV 也是大语言模型但不是 Transformer，只是目前 Transformer 是绝对主流。

### 时间线（谁先谁后）

```
2017   Transformer 诞生（《Attention Is All You Need》）
2018   BERT、GPT-1 用 Transformer 实现，效果碾压旧方法
2020+  模型越做越大 → 才出现"大语言模型(LLM)"的叫法
2022+  ChatGPT 走红 → LLM 进入大众视野
```

**结论：Transformer 是"因"，LLM 是"果"。**

---

## 六、五者关系定位表（对比）

| 维度 | Transformer | LLM | Prompt | RAG | ReAct |
|---|---|---|---|---|---|
| **层次** | 技术层（结构） | 产品层（模型） | 交互层（指令） | 应用层（知识） | 应用层（行动） |
| **生活类比** | 发动机 | 汽车 | 方向盘+指令 | 图书馆 | 手脚+办事流程 |
| **解决什么问题** | 怎么算词与词的关系 | 让 AI 能读能写 | 怎么指挥大脑干活 | 不知道的怎么办 | 要动手的怎么办 |
| **输入 → 输出** | 向量 → 向量 | 文字 → 文字 | 指令/资料 → 组织好的指令 | 问题 → 相关文档片段 | 任务 → 工具调用+答案 |
| **面试关键词** | 自注意力、并行 | 参数规模、涌现能力 | 角色、few-shot、约束 | 检索、向量库、防幻觉 | 思考-行动循环 |

> 记忆口诀：**Transformer 造 LLM，Prompt 指挥它，RAG 喂知识，ReAct 给手脚。**

---

## 七、具体例子：五个概念怎么配合

> 问 AI："公司食堂几点关门？"

| 方案 | 表现 |
|---|---|
| 只有 LLM（Transformer） | 凭记忆瞎编："一般是 6 点" —— 产生**幻觉** |
| LLM + Prompt | 明确指令"只准回答有依据的，不知道就说不知道" → 至少不乱编 |
| LLM + RAG | 先查公司文档库《食堂管理规定》第 3 条："20:30 关门" → 有据可查 |
| LLM + ReAct | 文档库没写 → 调 OA 系统 API 查实时数据 → 再回答 |
| LLM + Prompt + RAG + ReAct | 指令约束 + 检索文档（RAG）+ 不够再调 API（ReAct）→ 完整闭环 |

**关键点：RAG 和 ReAct 不是二选一，经常组合使用。**
ReAct 的循环里，可以把"检索文档库"当成一个工具调用，而每一步都是构造 Prompt 发给 LLM：

```
System Prompt: 你是公司助手，回答问题必须基于检索到的资料，禁止编造。

Thought: 用户问食堂几点关门，我不确定，先查一下
Action:  search_docs("食堂营业时间")      ← 这一步就是 RAG！
Observation: 《食堂管理规定》:20:30 关门
Thought: 好，知道了
Action:  finish("食堂 20:30 关门")
```

**配套可运行示例**：[`Agent完整闭环示例.py`](./Agent完整闭环示例.py)（零依赖纯 Python）
运行它可以看到三个真实场景的完整闭环：食堂查实时数据（RAG 无果 → 调 API）、报销查文档（RAG 命中直接答）、未知问题拒绝编造（Prompt 约束防幻觉），并且每一步"发给 LLM 的 Prompt"都会打印出来。

---

## 八、面试速答（背这段）

> **Q：Transformer 和 LLM 什么关系？**
> Transformer 是一种基于自注意力机制的神经网络结构，是绝大多数大语言模型（LLM）的底层实现技术。LLM 是按功能和规模分类的产品，Transformer 是按内部结构分类的技术。两者是交叉关系：绝大多数 LLM 用 Transformer 造的，但 Transformer 也能用于图像（ViT）、语音等领域，未来 LLM 也可能用新结构（如 Mamba）。
>
> **Q：Prompt 在 Agent 里是什么位置？**
> Prompt 是用户与 LLM 交互的指令层，也是连接外部能力与大脑的桥梁。RAG 的检索结果、ReAct 的格式约束、Function Calling 的工具描述，最终都要拼进 Prompt 才能让模型执行。Prompt 写得好不好，直接决定同样一个模型的效果上限，是 Agent 开发中成本最低、见效最快的优化点。
>
> **Q：RAG 和 ReAct 有什么区别？**
> RAG 是检索增强生成，解决"模型不知道的知识"——回答前先去外部文档库检索相关内容再回答，防止幻觉；ReAct 是推理+行动循环，解决"需要动手的任务"——模型边思考边调用工具，观察结果后继续，直到完成。RAG 可以看作 ReAct 工具箱里的一个"检索工具"，两者常组合使用。
>
> **Q：AI Agent 是什么？**
> Agent = LLM（Transformer 大脑）+ Prompt（指令系统）+ 规划与行动（ReAct）+ 知识检索（RAG）+ 记忆 + 工具调用。是一个能理解任务、查询知识、调用工具、分步完成目标的完整智能体。

---

## 九、常见追问

**Q1：为什么 LLM 会产生幻觉？**
LLM 本质是"预测下一个字"，它只有训练时的知识，没有"实时查证"能力。不知道的就会顺着概率编。RAG 就是给它在回答前加一道"查证"流程，Prompt 也能通过"不知道就说不知道"的约束减少幻觉。

**Q2：RAG 一定需要向量数据库吗？**
常规做法是：文档切块 → Embedding 转向量 → 向量数据库检索。也可以用关键词/混合检索，但向量检索的语义匹配效果最好。

**Q3：ReAct 和 Function Calling 啥关系？**
Function Calling 是 ReAct 中 Action 步骤的标准化协议版本。ReAct 靠 prompt 约定格式，Function Calling 由模型原生支持工具调用，更稳定。

**Q4：Prompt 和 RAG 有什么区别？**
RAG 是把外部知识"拿进来"的技术（去检索文档）；Prompt 是"怎么把内容组织给模型看"的方式。RAG 的结果最终也是拼进 Prompt 里喂给模型的——两者是"取数据"和"说话"的关系。

**Q5：先调 Prompt 还是换模型？**
先调 Prompt。同一个 LLM，Prompt 写得好（角色、few-shot、思维链、强约束）效果能提升一大截，而且免费、快；Prompt 调到头还不够，才考虑换更强模型或加 RAG/微调。

**Q6：五者哪个最重要？**
Transformer 是地基（没有它其他都谈不上）；LLM 是产品形态；Prompt 是唯一入口；RAG 和 ReAct 是能力扩展。面试时先讲底座，再讲入口，再讲扩展，最后串成 Agent。

**Q7：相关文件夹去哪看？**
- Transformer → [`transfromer/知识点.md`](./transfromer/知识点.md)
- Prompt → [`大模型怎么稳定输出JSON`](./大模型怎么稳定输出JSON/)（强约束 Prompt 实战）
- RAG → [`RAG/知识点.md`](./RAG/知识点.md)
- ReAct → [`ReAct/知识点.md`](./ReAct/知识点.md)
